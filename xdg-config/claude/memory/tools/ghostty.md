# Ghostty Terminal

## CLI Syntax

- Actions use `+action` prefix — e.g. `ghostty +list-fonts`, `ghostty +help`
- Always verify with `ghostty +help` before suggesting commands; syntax changes across versions
- Do NOT use flag-style syntax like `ghostty --list-fonts` — it will fail
