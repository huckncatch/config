# if [[ "$DEBUG_STARTUP" == "1" ]]; then
#   echo ${0:A}
# fi

# can be added to your shell profile (.zshrc, .bashrc, etc.) to make it permanent,
# or set it in your Claude Code settings.json file.
export DISABLE_AUTOUPDATER=1
